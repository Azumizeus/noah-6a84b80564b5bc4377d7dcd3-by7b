# Dependencies
node_modules

# Lockfiles
bun.lock

# Rust / Cargo build artifacts
target/debug
target/release
target/sbpf-solana-solana
target/.rustc_info.json

# Anchor
.anchor

# OS files
.DS_Store
.vercel
.env*

# Build output — on garde uniquement l'IDL et le .so. La keypair du programme
# ne doit JAMAIS être commitée (incidents 5-7) : elle reste hors repo.
target/
!target/idl/
!target/deploy/*.so
*keypair.json

# Clés privées — ne JAMAIS committer
wallet.json
target/wallet/
archive/wallets/
supabase/.temp/.DS_Store
wallet-new.json
logs/
