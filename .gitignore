# Dependencies
node_modules

# Lockfiles
bun.lock

# Rust / Cargo build artifacts
target/debug
target/release
target/sbpf-solana-solana
target/.rustc_info.json

# Anchor
.anchor

# OS files
.DS_Store
.vercel
.env*

# Build output — on garde uniquement l'IDL, le .so et la keypair du programme
target/
!target/idl/
!target/deploy/*.so
!target/deploy/*-keypair.json

# Clés privées — ne JAMAIS committer
wallet.json
target/wallet/
archive/wallets/
supabase/.temp/.DS_Store
wallet-new.json
logs/
